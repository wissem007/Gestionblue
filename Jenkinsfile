pipeline {
  agent any
  stages {
    stage('Git check Out') {
      steps {
        git(url: 'https://github.com/wissem007/nodejs-demo.git', branch: 'matser')
        echo 'Sucess pour le clonage github'
      }
    }

  }
}